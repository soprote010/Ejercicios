<?php

/*function dia($uno, $dos, $tres, $cuatro, $cinco)
{
	echo $uno;
}

dia("Lunes", "Martes", "Miercoles", "Jueves", "Viernes");*/

/*
1. Funciones de cadena/texto
	-strtoupper($variable); ->todo en mayuscula
	-strtolower($variable); ->todo en minusculas
	-ucfirst($variable); 	->capitalizar
	-ucwords($variable);	->Primera letra de cada palabra en mayuscula
	-strtr($variable);		->
	-strlen($variable);		->cantidad de letras
	-chr($variable);		->devuelve el codigo ASCII
	-ord($variable);		->devuelve el numero en el codigo ASCII
2. Funciones matematicas
	-is_int();				->comprueba si es entero
	-is_float();			->comprueba si es de coma flotante
	-is_numeric():			->comprueba si es numero
	-round($a, numeroDeRedondeo);->Redondea un valor
	-IntVal($a);			->Obtiene el valor del entero, sin redondeo
	-rand(5,15);			->Numeros aleatorios
	-min(2,9,7,6);			->Valor mas bajo
	-max(2,9,7,6);			->Valor mas alto
	-pow($valor,exponente);	->expresion exponencial
	-sqrt($numero);			->Raiz cuadrada

3.	Funciones de fechas
	-checkdate(_,_,_);		->Verifica si la fecha es correcta
	-date(_-_-_);			->Pone la fecha
		- L - devuelve el dia
		- m - mes numera con 0 inicial
		- Y - año
		- d - dia
		- w - el numero del dia comenzando en lunes
	- getdate();
		- seconds
		- minutes
		- hours
		- mday
		- wday
		- mon
		- year
		- yday
		- weekday
		- mounth
		- Ej: $a = getdate();
4.	Funciones de array
	- range(_,_);
	- count($variable);
	- in_array('rojo', $color);
	- unset($a[2]);
	- current();
	- next();
	- recorre();
	- prev();
	- end();
	- key();
	- array_flip();
	- array_reverse();
	- shuffle();
	- array_push();
	- array_pop();
	PARA ORDENAR
	- Asort() -asendente
	- Arsort() - descendent
	- ksort() - ascendente con el indice
	- krsort() - descendente con el indice
	- sort() - ascendente
	- rsort() - descendente
5. Funciones de control
	- isset() = determina si una variable esta DECLARADA (si existe) y si no es NULL
	-!isset()
	- empty = determina si una variable esta vacia
	- !empty
	- unset() = destruye una variable
	- is_null() = 
	- is_integer();
	- is_double();
	- is_string();
	- intval();
	- doubleval();
	- strval();
	- request['variable']
6. Funciones de acabado o finalizacion
	- exit();
	- die()
	- endif
	- endwhile
	- endfor
	- endwith
7. Funciones de insercion
	- include('nombreDocumento');
	- include_once('nombreDocumento');
	- require('nombreDocumento');
	- require_once('nombreDocumento')

*/
?>